# Core build pipeline (build.py / build_utils.py / assemble_video.py / etc.)
opencv-python-headless>=4.8
numpy>=1.24
Pillow>=10.0

# Lyric OCR extraction scripts (extract_*.py) — optional, heavier install.
# Uncomment if you need to run the OCR extractors:
# easyocr>=1.7
